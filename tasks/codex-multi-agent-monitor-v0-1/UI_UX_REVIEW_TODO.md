# UI/UX Review TODO

Reviewed: 2026-03-19 (8th pass — saturated)
Scope: `src/shared/ui/`, `src/features/`, `src/theme/`, `src/app/`, Storybook stories vs UX_SPEC.md / UX_BEHAVIOR_ACCESSIBILITY.md

---

## P0 --- Usability Degradation

- [ ] [accessibility] InspectorTabs uses `role="tab"` but no matching `role="tabpanel"` or `aria-controls`/`id` pairing exists anywhere in the codebase --- related file: `src/shared/ui/InspectorTabs.tsx` (line 24), `src/features/inspector/CausalInspectorPane.tsx`
- [ ] [accessibility] ResizeHandle renders as `<button>` but has no `role="separator"` and no `aria-valuenow`/`aria-valuemin`/`aria-valuemax` ARIA attributes; the CSS in primitives.css defines focus-visible for `[role="separator"]` but the component never applies that role --- related file: `src/app/MonitorApp.tsx` (lines 465-506)
- [ ] [color] `--color-waiting` (#F5C451) on `--color-bg` (#0F1115) yields approximately 8.5:1 which is fine, but `--color-text-tertiary` (#737B88) on `--color-surface` (#1A1F2B) yields roughly 3.4:1 --- below the 4.5:1 minimum for normal text. Tertiary text is used extensively in `.event-row__meta`, `.metric-pill__label`, `.run-row__meta`, `.graph-sequence__time-stack span` --- related file: `src/theme/tokens.css` (line 13), `src/theme/primitives.css`
- [ ] [color] `--color-text-secondary` (#A6AFBD) on `--color-panel` (#141821) yields roughly 4.8:1 which barely passes, but `.graph-sequence__card-summary` uses `rgba(255,255,255,0.55)` (effectively ~#8c8c8c on dark bg) which drops below 4.5:1 on the card background --- related file: `src/app/app.css` (line 861)
- [ ] [accessibility] EventRow is a `<button>` with no keyboard-specific indicator showing it is the currently selected item within a list; there is no `aria-current` or `aria-selected` attribute on the selected row, and no `role="listbox"` / `role="option"` structure for the event column --- related file: `src/shared/ui/EventRow.tsx` (lines 10-33)
- [ ] [accessibility] Inspector drawer tabs in MonitorApp.tsx (line 545-556) do not use `role="tablist"` / `role="tab"` / `role="tabpanel"` --- the same pattern InspectorTabs.tsx uses for Inspector, but drawer tabs are raw buttons without ARIA tab semantics --- related file: `src/app/MonitorApp.tsx` (lines 544-557)
- [ ] [accessibility] Shortcut dialog uses `<dialog open>` which does not trap focus or announce itself as a modal to screen readers; missing `aria-modal="true"` and focus trap logic --- related file: `src/app/MonitorApp.tsx` (lines 274-288)
- [ ] [interaction] No `:active` pseudo-class styles exist anywhere in the codebase; button press feedback is entirely absent for all interactive controls (buttons, event cards, jump buttons, gap chips, tabs) --- related file: `src/theme/primitives.css`, `src/app/app.css`

## P1 --- Experience Improvement

- [ ] [spec-deviation] UX_SPEC specifies shortcut `G` for graph mode, `W` for waterfall mode, `M` for map mode, `F` for filter focus, `Cmd/Ctrl+K` for command palette --- none of these are implemented in MonitorApp keyboard handler; only `/`, `I`, `.`, `E` are wired --- related file: `src/app/MonitorApp.tsx` (line 104-117)
- [ ] [spec-deviation] UX_SPEC specifies mode tabs (Graph / Waterfall / Map) in the top bar area of SCR-002; the current implementation has no mode switching UI or state --- waterfall and map modes are only partially stubbed in Storybook stories via keydown dispatch --- related file: `src/app/MonitorApp.stories.tsx` (lines 21-27), `src/app/MonitorApp.tsx`
- [ ] [spec-deviation] UX_SPEC specifies InspectorTabs with `Summary`, `Input`, `Output`, `Trace`, `Raw` tabs. CausalInspectorPane does not use InspectorTabs at all --- it renders fixed sections (Summary, Direct cause, Upstream, Downstream, Payload) without a tabbed interface --- related file: `src/features/inspector/CausalInspectorPane.tsx`, `src/shared/ui/InspectorTabs.tsx`
- [ ] [layout] UX_SPEC specifies default pane widths as `left rail 280px / main canvas flexible / inspector 360px`. Current implementation delegates initial widths to `useMonitorAppState` which is not audited here, but the ResizeHandle has no min/max constraints visible in CSS --- rail and inspector can be dragged to zero or beyond viewport --- related file: `src/app/MonitorApp.tsx` (lines 465-506)
- [ ] [interaction] Node hover causal path highlighting (upstream/downstream dim) specified in UX_BEHAVIOR_ACCESSIBILITY.md Interaction Model is not implemented; graph cards have only hover border-color change but no lane dimming or causal path highlight on hover --- related file: `src/app/app.css` (lines 815-819), `src/features/run-detail/graph/CausalGraphView.tsx`
- [ ] [interaction] Edge hover tooltip and edge focus state are missing on the visible SVG layer; edge interaction only exists on the transparent hitbox overlay without visual feedback on the rendered edge path --- related file: `src/features/run-detail/graph/CausalGraphView.tsx` (lines 430-458), `src/app/app.css` (lines 911-917)
- [ ] [typography] Metric values in `.metric-pill__value`, `.graph-sequence__time-stack`, and `.drawer__footer` lack `font-variant-numeric: tabular-nums` causing numbers to shift horizontally when values update (e.g., during live mode) --- related file: `src/theme/primitives.css` (lines 189-208), `src/app/app.css` (lines 245-247)
- [ ] [spacing] `.monitor-shell` uses `padding: 18px` which is not aligned to the 8px grid (should be 16px or 24px) --- related file: `src/app/app.css` (line 8)
- [ ] [spacing] `.event-row__headline` uses `margin-bottom: 6px` which is not on the 4px/8px grid --- related file: `src/theme/primitives.css` (line 278)
- [ ] [spacing] `.lane-header__meta` uses `margin-top: 4px` which is fine, but `.prompt-assembly` uses `gap: 6px` and `.graph-sequence__time-stack` uses `gap: 6px` which deviate from the 4px/8px grid tokens --- related file: `src/features/inspector/promptAssembly.css` (line 5), `src/app/app.css` (line 761)
- [ ] [motion] `skeleton-pulse` animation in `.archive-list__skeleton` has no `prefers-reduced-motion: reduce` override --- related file: `src/app/app.css` (lines 1074-1077)
- [ ] [motion] `.layer-card` border-color transition (`transition: border-color 0.15s ease`) and `.layer-card__chevron` rotation transition have no `prefers-reduced-motion: reduce` suppression --- related file: `src/features/inspector/promptAssembly.css` (lines 34, 122)
- [ ] [interaction] PromptAssemblyView layer card header uses `<div role="button">` for expand/collapse but there is no visible focus ring defined for `.layer-card__header:focus-visible` in promptAssembly.css --- related file: `src/features/inspector/PromptAssemblyView.tsx` (line 59), `src/features/inspector/promptAssembly.css`
- [ ] [microcopy] GapChip shows `Show / Hide` toggle text, but UX_SPEC gap copy format specifies `// 18m 24s hidden . 4 lanes idle //` --- the GapChip label passed from selectors follows this, but the toggle prefix `Show . / Hide .` differs from the spec's `//` delimiter convention --- related file: `src/shared/ui/GapChip.tsx` (line 21)
- [ ] [density] Graph card height is fixed at 80px (`CARD_HEIGHT = 80`), and EVENT_ROW_HEIGHT is 132px. For dense runs (FIX-004: 120+ events), this produces a very tall scrollable area (~16,000px+). No compact card mode or dynamic row height reduction is implemented for high-density scenarios --- related file: `src/features/run-detail/graph/graphLayout.ts` (lines 3-6, 11)
- [ ] [hierarchy] SummaryStrip uses inline layout with MetricPills at the same visual weight as the focus label; no typographic hierarchy differentiates the active selection context from the metric values --- related file: `src/app/MonitorApp.tsx` (lines 352-369), `src/app/app.css` (lines 206-224)
- [ ] [state] `interrupted` and `cancelled` StatusChip glyphs both use the same shape (rounded square with gradient `linear-gradient(180deg, var(--color-transfer), var(--color-active))`) making them visually indistinguishable --- related file: `src/theme/primitives.css` (lines 142-146)
- [ ] [state] `stale` and `disconnected` glyphs both render as plain filled circles differing only in color (`--color-stale` #FB7185 vs `--color-disconnected` #94A3B8); the accessibility contract says color alone must not be the only distinguisher --- related file: `src/theme/primitives.css` (lines 161-167)
- [ ] [모션] `.run-list__disclosure` has `transition: transform 0.15s ease` but is not covered by any `prefers-reduced-motion: reduce` media query (neither `motion.css` nor `app.css` reduced-motion blocks include it) --- related file: `src/app/app.css` (lines 362-363)
- [ ] [접근성] ArchivedSessionList workspace rows render as plain `<button>` without `role="treeitem"` while WorkspaceRunTree uses `role="treeitem"` with `aria-level` and `aria-expanded`; this creates inconsistent tree ARIA semantics for screen readers navigating the archive section --- related file: `src/features/run-list/ArchivedSessionList.tsx` (lines 95-113)
- [ ] [접근성] Import drawer `<textarea>` has no `aria-label` or associated `<label>` element; screen readers cannot identify its purpose --- related file: `src/app/MonitorApp.tsx` (lines 590-593)
- [ ] [계층구조] `buildSummaryFacts` computes `emphasis` values ("warning", "danger", "accent") for summary facts but `MetricPill` component does not accept or render emphasis; blocked-by warnings, failure indicators, and normal metrics all appear at identical visual weight in SummaryStrip --- related file: `src/shared/domain/selectors.ts` (lines 628-633), `src/shared/ui/MetricPill.tsx`, `src/app/MonitorApp.tsx` (lines 364-366)
- [ ] [상태] `queued` and `cancelled` statuses have CSS glyphs defined (`status-chip__glyph--queued`, `status-chip__glyph--cancelled`) and type declarations, but no fixture dataset demonstrates either state; visual rendering of these glyphs is untestable --- related file: `src/theme/primitives.css` (lines 107-117, 142-146), `src/features/fixtures/runFixtures.ts`
- [ ] [접근성] Live connection transitions (live → stale → disconnected → reconnected) update the `.live-badge` text visually but have no `aria-live` region; screen readers are not notified of connection state changes. Only `GapDetailSection` uses `aria-live="polite"` --- related file: `src/app/MonitorApp.tsx` (lines 324-328)
- [ ] [색상] Three CSS custom properties are referenced but never defined in `tokens.css`: `--color-text-primary` (used in `promptAssembly.css:108`), `--color-text-muted` (used in `app.css:563,861`), `--color-surface-raised` (used in `promptAssembly.css:115`). The first silently falls back to inherited color; the latter two rely on inline `var()` fallback values bypassing the token system --- related file: `src/theme/tokens.css`, `src/app/app.css` (lines 563, 861), `src/features/inspector/promptAssembly.css` (lines 108, 115)
- [ ] [밀도] `.storybook/preview.ts` does not import global CSS files (`tokens.css`, `primitives.css`, `motion.css`, `app.css`); these are only imported in `main.tsx` which is not the Storybook entry point. MonitorApp stories render without design tokens, layout styles, or component primitives — making visual review of stories non-functional --- related file: `.storybook/preview.ts`, `src/main.tsx` (lines 3-6)
- [ ] [접근성] `<main>`, both `<aside>` (rail, inspector), and `<header>` (top-bar) landmark elements have no `aria-label`; screen readers announce them as unnamed landmarks making it impossible to distinguish the run list rail from the inspector sidebar or navigate by landmark --- related file: `src/app/MonitorApp.tsx` (lines 171, 200, 253, 309)

## P2 --- Polish

- [ ] [typography] `.panel__title` uses `font-size: 0.95rem` which does not land on a clean type scale step; consider aligning to a modular scale or at least a round rem value --- related file: `src/theme/primitives.css` (lines 57-59)
- [ ] [typography] `.graph-sequence__role-badge` uses `font-size: 0.68rem` and `.graph-sequence__time-stack span` uses `font-size: 0.68rem` which is below the typical minimum readable size of 0.7rem on desktop --- related file: `src/app/app.css` (lines 565, 767)
- [ ] [spacing] `.shortcut-dialog ul` uses `padding-left: 18px` (not on 8px grid) and `margin: 14px 0 18px` (14px and 18px are off-grid) --- related file: `src/app/app.css` (lines 1315-1319)
- [ ] [spacing] `.top-bar__identity` uses `gap: 6px`, `.inspector-section` uses `gap: 8px` but `.inspector-jump` uses `gap: 3px` --- the 3px and 6px values break the 4px grid base --- related file: `src/app/app.css` (lines 26, 1139, 1177)
- [ ] [layout] `.graph-sequence__scroll` hides the scrollbar entirely (`scrollbar-width: none` / `::-webkit-scrollbar { display: none }`) which removes scroll position affordance and makes it difficult to judge how far through the graph the user has scrolled --- related file: `src/app/app.css` (lines 491-500)
- [ ] [interaction] `.run-list__workspace-row` and `.run-row` have hover styles but no `:focus-visible` style; keyboard users navigating the tree see only the browser's default outline from primitives.css global rule, which may not match the component's visual language --- related file: `src/app/app.css` (lines 333-343, 422-443)
- [ ] [interaction] `.inspector-jump` button has no `:hover` or `:focus-visible` style defined --- related file: `src/app/app.css` (lines 1175-1184)
- [ ] [microcopy] `formatTokens` produces `12.4k tok` but the UX_SPEC says `12.4k tok` which matches; however `formatCurrency` always produces `$0.42` for small values but does not switch to `$0.004` when cost is below one cent (it shows `$0.00` for values < 0.005 due to `.toFixed(2)`) --- related file: `src/shared/domain/format.ts` (lines 21-31)
- [ ] [microcopy] `truncateId` truncates to `8...3` format producing e.g. `trace_7f...91c` but the UX_SPEC example shows `trace_7f2...91c` (first 9 chars); the current slice is `slice(0,8)...slice(-3)` --- related file: `src/shared/domain/format.ts` (lines 83-89)
- [ ] [color] `.graph-sequence__route` default color is `rgba(166, 175, 189, 0.34)` which at 34% opacity on the dark background yields very low visibility (roughly 1.5:1 contrast ratio); non-interactive edges should still meet the 3:1 non-text contrast minimum --- related file: `src/app/app.css` (line 629)
- [ ] [hierarchy] Drawer title is hardcoded as `"Bottom drawer"` which is a technical label rather than a contextual title; it should reflect the active tab name or selection context --- related file: `src/app/MonitorApp.tsx` (line 537)
- [ ] [layout] `@media (max-width: 1180px)` breakpoint collapses the inspector to full-width below the main content, but this threshold is higher than the UX_SPEC's minimum of 1024px; between 1024px-1180px the inspector is collapsed when it could still fit alongside the main canvas --- related file: `src/app/app.css` (line 1321)
- [ ] [density] `.archive-list__tree` has `max-height: 360px` as a hardcoded limit; on tall viewports this wastes available space, and on short viewports it may still cause a nested scroll region that competes with the main run list scroll --- related file: `src/app/app.css` (line 1049)
- [ ] [여백] `.gap-chip` uses `gap: 5px` and `.run-row__sub` uses `gap: 5px` — 5px is off the 4px/8px grid --- related file: `src/app/app.css` (lines 871, 389)
- [ ] [여백] `.run-row` padding `5px 8px` and `.gap-detail-section__event-button` padding `5px 8px` — 5px vertical padding is off-grid --- related file: `src/app/app.css` (lines 428, 973)
- [ ] [여백] `.layer-card__header` uses `gap: 10px` and `padding: 10px 14px` — 10px and 14px are both off 8px grid (should be 8/12px and 12/16px) --- related file: `src/features/inspector/promptAssembly.css` (lines 93-94)
- [ ] [여백] `.graph-sequence__card` uses `padding: 12px 14px` and `.graph-sequence__card-head` uses `gap: 10px` — 14px and 10px are off 8px grid --- related file: `src/app/app.css` (lines 800, 835)
- [ ] [여백] `.graph-sequence__card` uses `border-radius: 14px` which is not a defined radius token (`--radius-panel: 12px`, `--radius-soft: 10px`, `--radius-pill: 999px`) --- related file: `src/app/app.css` (line 802)
- [ ] [타이포] `.graph-sequence__tool-badge` uses `font-size: 0.68rem` — same sub-minimum issue as `.graph-sequence__role-badge` (below 0.7rem readable threshold) --- related file: `src/app/app.css` (line 1389)
- [ ] [타이포] `promptAssembly.css` uses `px` units for font-size (`12px`, `14px`, `11px`) while the rest of the codebase consistently uses `rem` — mixed unit system creates maintenance burden --- related file: `src/features/inspector/promptAssembly.css` (lines 17, 107, 113)
- [ ] [인터랙션] `.gap-detail-section__summary` has `cursor: pointer` but no `:hover` or `:focus-visible` styles defined; custom `<summary>` replaces native disclosure marker but lacks visual feedback --- related file: `src/app/app.css` (lines 934-948)
- [ ] [인터랙션] `.archive-section__header` has `:hover` style but no `:focus-visible` style for keyboard users --- related file: `src/app/app.css` (lines 1009-1025)
- [ ] [여백] `EVENT_ROW_HEIGHT = 132` is not aligned to 8px grid (nearest: 128 or 136); affects all graph row positioning calculations --- related file: `src/features/run-detail/graph/graphLayout.ts` (line 4)
- [ ] [마이크로카피] `formatTimestamp` uses `Intl.DateTimeFormat` with `en-US` locale and `hour: "numeric"` producing 12-hour AM/PM format (e.g. "2:15:30 PM"); a technical observability tool typically uses 24-hour format for unambiguous readability --- related file: `src/shared/domain/format.ts` (lines 49-55)
- [ ] [밀도] No Storybook stories exist for any primitive component in `src/shared/ui/` (`StatusChip`, `MetricPill`, `LaneHeader`, `EventRow`, `GapChip`, `InspectorTabs`, `EventTypeGlyph`, `Panel`); stories are only defined for `MonitorApp` making individual component state review impossible --- related file: `src/shared/ui/`
- [ ] [마이크로카피] Keyboard shortcut `C` (opens context drawer) is wired in `useMonitorAppState.ts` (line 503) but not listed in the shortcut help dialog or in UX_SPEC; users cannot discover this shortcut --- related file: `src/app/useMonitorAppState.ts` (line 503), `src/app/MonitorApp.tsx` (lines 277-283)
- [ ] [계층구조] Shortcut dialog `<dialog open>` renders with no backdrop or overlay dimming; it floats over the complex graph background reducing dialog content readability and lacking modal affordance --- related file: `src/app/MonitorApp.tsx` (line 275), `src/app/app.css` (lines 1305-1312)
- [ ] [색상] `src/styles.css` (starter UI) defines `:root { font-family: "Avenir Next" }` and warm color palette (#f6efe8, #1f1614) that conflict with production `tokens.css` (`IBM Plex Sans`, #0F1115); while not imported by `main.tsx`, the file remains a latent conflict risk if loaded by Storybook or other entry points --- related file: `src/styles.css` (lines 1-7), `src/theme/tokens.css` (lines 2-5)
- [ ] [밀도] No empty state design exists for when the run list contains zero runs; `WorkspaceRunTree` renders an empty tree with no placeholder message, guidance, or import CTA when `model.workspaces` is empty --- related file: `src/features/run-list/WorkspaceRunTree.tsx` (lines 214-283)
- [ ] [타이포] No global `line-height` is set on `body` or `:root` in the production CSS (`tokens.css`, `primitives.css`); the starter `styles.css` sets `line-height: 1.5` but is not imported by `main.tsx`. Only 5 elements set explicit `line-height` (1.12–1.6), leaving most text content at browser default (~1.2) which is tight for multi-line readability --- related file: `src/theme/tokens.css`, `src/main.tsx` (lines 3-6)
- [ ] [레이아웃] z-index values are ad-hoc across the codebase (0, 1, 2, 3, 4, 10) with no token or scale documentation; `.drawer--open` z-index:10 overlaps with `.graph-sequence__lane-strip` z-index:4 in the same stacking context, and future additions risk unintended overlap without a defined layer system --- related file: `src/app/app.css` (lines 505, 518, 593, 598, 700, 807, 1253)
- [ ] [여백] `.button` uses `padding: 9px 12px` — 9px vertical padding is off the 4px/8px grid (should be 8px or 12px) --- related file: `src/theme/primitives.css` (line 64)
- [ ] [여백] `.status-chip` uses `padding: 6px 10px` and `.tabs__button`/`.tabs__pill` uses `padding: 6px 10px` — both 6px and 10px are off the 8px grid --- related file: `src/theme/primitives.css` (lines 89, 307)
- [ ] [여백] `.event-row__body` uses `gap: 6px` — off the 4px/8px grid (same class of issue as `.event-row__headline` margin-bottom) --- related file: `src/theme/primitives.css` (line 283)
- [ ] [여백] `.gap-chip` in app.css overrides primitives padding with `padding: 0 14px` — 14px is off 8px grid and not a defined spacing token --- related file: `src/app/app.css` (line 875)
- [ ] [색상] `.graph-sequence__card--llm-started` applies `opacity: 0.8` and `.graph-sequence__card--turn-started/finished` applies `opacity: 0.6`; combined with the already-low contrast of `.graph-sequence__card-summary` text (rgba 0.55), these opacity reductions compound the text readability failure on affected card variants --- related file: `src/app/app.css` (lines 1357, 1379, 861)
- [ ] [타이포] The codebase uses 16 distinct `font-size` values with no modular type scale: 0.68, 0.7, 0.72, 0.73, 0.74, 0.75, 0.76, 0.78, 0.8, 0.82, 0.92, 0.95rem plus clamp() and 3 px values in promptAssembly.css. Steps range from 0.01rem to 0.1rem with no consistent ratio (e.g. Major Third 1.25 or Perfect Fourth 1.333), creating micro-hierarchy noise where adjacent sizes are visually indistinguishable --- related file: `src/theme/primitives.css`, `src/app/app.css`, `src/features/inspector/promptAssembly.css`

## Spec Mismatch

- [ ] [spec-deviation] UX_SPEC specifies `selected row fill radius 8` but `.event-row--selected` uses `border-radius: var(--radius-soft)` which is 10px --- related file: `src/theme/primitives.css` (line 260-263), `src/theme/tokens.css` (line 30)
- [ ] [spec-deviation] UX_BEHAVIOR_ACCESSIBILITY specifies `pointer hit target minimum 32px` for main interactive controls; `.gap-detail-section__collapse` has `min-height: 24px` and `.status-chip--subtle` at `padding: 4px 8px` may produce hit targets below 32px --- related file: `src/app/app.css` (line 950-955), `src/theme/primitives.css` (lines 97-99)
- [ ] [spec-deviation] UX_BEHAVIOR_ACCESSIBILITY specifies focus return must go back to the invoking row/node after closing inspector drawer, context menu, or command palette. The drawer close (`closeDrawer`) does restore focus to `drawerTriggerRef`, but the shortcut dialog close does not track or restore focus at all --- related file: `src/app/MonitorApp.tsx` (lines 274-288)
- [ ] [spec-deviation] UX_BEHAVIOR_ACCESSIBILITY specifies `hover-only information must not exist`; hover tooltip on `.graph-sequence__card` edge interactive overlay `<title>` element content is not available on keyboard focus --- the `<a>` element has `aria-label` but the `<title>` text content (which shows `edgeType: label`) has no focus-equivalent display --- related file: `src/features/run-detail/graph/CausalGraphView.tsx` (lines 443-456)
- [ ] [spec-deviation] UX_BEHAVIOR_ACCESSIBILITY specifies `waiting, blocked, interrupted states must show text state label AND wait reason together`; EventRow shows `wait_reason` callout only if `waitReason` is truthy, but graph cards in CausalGraphView never render `waitReason` --- the card summary may contain it only if it is the first non-null field in `summary: event.waitReason ?? event.outputPreview ?? event.inputPreview ?? "n/a"` --- related file: `src/features/run-detail/graph/CausalGraphView.tsx` (lines 409-419)
- [ ] [spec-deviation] UX_SPEC specifies `sticky event column 280px-320px range`; the current graph layout uses `TIME_GUTTER = 148px` for the time column but there is no separate sticky event column --- the time gutter and lane-cell grid is the entire layout --- related file: `src/features/run-detail/graph/graphLayout.ts` (line 3)
- [ ] [spec-deviation] UX_SPEC specifies `panel padding 12px, section padding 16px`; `.panel.run-list` and `.panel.inspector` use `padding: 14px` which does not match either 12px or 16px --- related file: `src/app/app.css` (line 153)
- [ ] [spec-deviation] UX_BEHAVIOR_ACCESSIBILITY specifies `live badge states: stale (5s no events), disconnected (20s+ no events)` with specific badge text. The implementation renders `liveConnection` directly as text (e.g., `"paused"`, `"stale"`) but the spec says `"Following paused"` for paused and the live badge class mapping exists for `paused`, `stale`, `disconnected`, `reconnected` --- only `"paused"` gets the label override to `"Following paused"`, while `"reconnected"` has no defined label --- related file: `src/app/MonitorApp.tsx` (lines 324-328)
- [ ] [spec-deviation] UX_SPEC specifies `top border-radius panel 12` but `.top-bar` uses `border-radius: 16px 16px 0 0` which is 16px, not 12px --- related file: `src/app/app.css` (line 17)
- [ ] [spec-deviation] LaneHeader component exists in `src/shared/ui/LaneHeader.tsx` but is not used anywhere; the graph view builds its own lane header markup inside CausalGraphView directly --- related file: `src/shared/ui/LaneHeader.tsx`, `src/features/run-detail/graph/CausalGraphView.tsx` (lines 208-224)
- [ ] [spec-deviation] EventRow component exists in `src/shared/ui/EventRow.tsx` but is not used anywhere in the current implementation; the graph view uses inline card markup instead --- related file: `src/shared/ui/EventRow.tsx`, `src/features/run-detail/graph/CausalGraphView.tsx` (lines 388-421)
- [ ] [spec-deviation] UX_BEHAVIOR_ACCESSIBILITY specifies keyboard focus order as `top bar -> left rail -> anomaly jump bar -> event column -> graph canvas -> inspector -> bottom drawer`; the current implementation has no explicit `tabindex` management or focus zones to enforce this order --- related file: `src/app/MonitorApp.tsx`
- [ ] [spec-deviation] Shortcut dialog lists `Cmd/Ctrl + K` as "help" but UX_SPEC Keyboard + Focus Contract specifies `Cmd/Ctrl + K: command palette`; also `?` should map to shortcuts help per spec but is not wired in the keyboard handler --- related file: `src/app/MonitorApp.tsx` (line 282)
- [ ] [spec-deviation] Initial pane widths `railWidth: 236px` and `inspectorWidth: 288px` do not match UX_SPEC defaults of `left rail 280px` and `inspector 360px`; resize minimum constraints (`220px` rail, `256px` inspector) have no maximum, allowing panes to exceed viewport --- related file: `src/app/useMonitorAppState.ts` (lines 148-149, 614-621)
- [ ] [spec-deviation] UX_SPEC SCR-001 specifies left rail groups as `Running`, `Waiting`, `Recent`, `Empty` status categories; current `buildWorkspaceTreeModel` groups by workspace name with no status-based grouping or section headers --- related file: `src/shared/domain/selectors.ts` (lines 475-568), `src/features/run-list/WorkspaceRunTree.tsx`
- [ ] [spec-deviation] `buildWorkspaceTreeModel` computes `quickFilters` array with `All`, `Live`, `Waiting`, `Failed` counts (selectors.ts lines 483-500) and returns them in `WorkspaceTreeModel`, but `WorkspaceRunTree` never renders quick filter pills — the data model supports status filtering but the UI discards it --- related file: `src/shared/domain/selectors.ts` (lines 483-500), `src/features/run-list/WorkspaceRunTree.tsx`
- [ ] [spec-deviation] UX_BEHAVIOR_ACCESSIBILITY state matrix specifies `empty`, `loading`, `cancelled`, `queued` states must all be covered; no fixture or UI path demonstrates `empty` (zero runs), `cancelled` (no event uses this status), or `queued` (title says "queued" but status is actually "waiting") --- related file: `src/features/fixtures/runFixtures.ts`, `tasks/codex-multi-agent-monitor-v0-1/UX_BEHAVIOR_ACCESSIBILITY.md` (lines 52-59)
- [ ] [spec-deviation] UX_BEHAVIOR_ACCESSIBILITY specifies `pointer hit target minimum 32px`; in addition to `.gap-detail-section__collapse` (24px, already noted), `.run-list__workspace-row` (min-height:28px), `.gap-chip` (min-height:28px), and `.archive-section__header` (min-height:28px) also fall below the 32px minimum --- related file: `src/app/app.css` (lines 337, 872, 1013)
